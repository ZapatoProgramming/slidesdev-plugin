// OpenCode V2 loads a local plugin from a directory's index.js; git/npm installs
// resolve through package.json "main". Both point at the same adapter.
export { default } from './.opencode/plugins/slides.js'
